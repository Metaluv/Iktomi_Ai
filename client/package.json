{
  "name": "Iktomi-the-storyteller",
  "version": "0.1.0",
  "description": "A simple Node.js app that uses OpenAI's GPT-3 API to generate stories.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
